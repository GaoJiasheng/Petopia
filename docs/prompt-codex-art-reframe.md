# Codex 任务：宠物动作帧「截断/邻帧渗漏」完全重做

## 背景与审计结论
用脚本 `tools/audit_clipping.py` 逐帧扫描了全部宠物美术（底图立绘 + 动作序列帧），并人工复核：

- ✅ **底图立绘**（`pet_<sp>_var01..05_stageA..D.png`，12 物种 × 20）：**全部合格**，无截断，**不要动**。
- ✅ **动作条 8 个物种合格**：`cat / cham / ember / parrot / rabbit / shiba / turtle / uni` —— 每帧一只完整动物、居中、四周留白足。**这几个是标杆，不要动。**
- ❌ **动作条 4 个物种有缺陷，必须完全重做**：`boo / hamster / snake / starbug`。

### 缺陷本质（看红框对比图后确认）
这 4 个物种的动作条，**每个 512×512 帧里除了主体，左/右边缘还渗进了相邻帧的动物残片**（例：snake 帧内右边缘出现下一帧蛇的橙色碎块；hamster 左边出现半只）。也就是**主体溢出 512 槽位 / 切片错位**，动画播放时边缘会闪现残影碎片，很脏。

## 需要重做的文件（仅这 4 物种的全部 8 个动作）
对 `boo / hamster / snake / starbug` 四个物种，各自 `assets/art/pets/<sp>/actions/` 下的 **8 个动作条**全部重做：
`idle, eat, pat, play, bath, sit, sleep, walk`
文件名两套都要（保持现有命名）：`pet_<sp>_var01_stageC_<action>.png` 与 `pet_<sp>_stageC_<action>.png`。

## 硬性质量要求（务必全部满足）
1. **规格**：每条 `4096×512`、横向 **8 帧**、每帧 **512×512**、透明底 PNG。
2. **一帧一只**：每个 512 帧内**有且仅有一只完整动物**，**绝不允许出现第二只、邻帧残片、任何切片碎块**。
3. **安全边距**：动物**完整**且四周留 **≥8%（约 40px）透明边距**——头/耳/尾/脚在**所有 8 帧**都不得触碰或超出帧边（含运动最大幅度的帧）。居中构图。
4. **主体不溢出**：动物在单帧内画完，**不得跨越到相邻帧槽位**。
5. **风格对齐标杆**：奶油卡通水彩、圆钝可爱，与现有 `cat / ember` 的动作条**同一完成度与画风**（可参考 `assets/art/pets/cat/actions/`、`assets/art/pets/ember/actions/`）。保持各物种既有角色设定（boo=小幽灵、hamster=仓鼠、snake=蛇、starbug=星形萤火虫）。
6. **动作语义**：eat=进食(带食盆)、pat=被摸头(带手/心)、play=玩球、bath=泡澡(浴盆)、sleep=蜷睡、sit/idle/walk 各自合理；8 帧循环连贯。

## ⛔ 铁律
- **只重做上面列的 4 物种 × 8 动作条**。其它一切美术（8 个合格物种、底图立绘、图鉴、明信片、UI 图标、App 图标、音频）**一个文件都不许碰**。
- 不改任何 `lib/**` 代码、不改 `pubspec.yaml`。
- 重做完在 `assets/art/qa/` 出一张接触表证明「每帧一只、四周留白、无残片」，供复核。

## 附：脚本审计明细（供核对）
运行 `python3 tools/audit_clipping.py` 可复现。当前被标记的帧（左/右=该侧边缘有实心内容渗漏）：

```
boo:     idle/eat/pat/sit/bath 帧1-5、play/sleep 帧2-5、walk 帧2-4  左右渗漏
hamster: 8 动作几乎每帧(0-5) 左/右渗漏
snake:   8 动作 帧1-5 及 帧8-9 左/右渗漏
starbug: 8 动作 帧0-5 左/右渗漏
```
重做后再跑一次脚本应**零命中**。
